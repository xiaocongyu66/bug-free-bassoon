{
  "name": "github-releases-proxy-worker",
  "version": "1.0.0",
  "description": "GitHub Releases代理服务 - Cloudflare Worker版本",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}